[build]
  base = "middle-messenger"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.19.0"

